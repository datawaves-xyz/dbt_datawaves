packages:
  - package: dbt-labs/dbt_utils
    version: 0.8.4

  - git: "https://github.com/datawaves-xyz/dbt_ethereum_source"
    # Ignore version control for now and add it when the dbt_ethereum_source has the stable version.
    # revision: 0.1.0
